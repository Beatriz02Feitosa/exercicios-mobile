import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  titulo:{
    fontSize: 24,
    color: 'black',
    textAlign: 'center',
    marginTop: 46,
    fontWeight: 'bold'
  },

})

export {styles}